<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tester Login - Smart Scheme</title>
    <style>
        /* Use the same styling as your admin panel */
        body {
            font-family: 'Montserrat', sans-serif;
            background: linear-gradient(to right, #e8f7fa, #d8effc);
            color: #2C3E50;
        }

        .container {
            max-width: 400px;
            margin: 0 auto;
            padding: 20px;
        }

        header {
            text-align: center;
            margin-bottom: 30px;
        }

        .form-container {
            background: white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: 500;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 4px;
        }

        button {
            width: 100%;
            padding: 12px;
            background: #3db17d;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        button:hover {
            background: #2596be;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Tester Login</h1>
        </header>
        <div class="form-container">
            <form action="/official-login" method="POST">
                <div class="form-group">
                    <label for="testerId">Tester ID</label>
                    <input type="text" id="testerId" name="testerId" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <button type="submit">Login</button>
            </form>
        </div>
    </div>
</body>
</html>